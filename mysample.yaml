---
- hosts: all
  become: yes
  tasks: 
  - name: install git 
    apt:
      name: "{{mygit}}"
      state: present
      
  - name: install tree
    apt:
      name: "{{mytree}}"    
      state: present

